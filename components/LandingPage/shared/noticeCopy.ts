/**
 * Single source for the notice copy that appears twice: once in the desktop
 * `ADABanner` / `EventPromoToast` and once inside `MobileNoticeDock`.
 */
export const NOTICE_EMAIL = "info@innovarehp.com";

export const NOTICE_EMAIL_LABEL = `Send an email to ${NOTICE_EMAIL} (opens email application)`;

export const ADA_NOTICE_BEFORE_EMAIL =
  "We are committed to ADA compliance and aim to make our website accessible to all users. Email ";

export const ADA_NOTICE_AFTER_EMAIL = " if you encounter any issues.";

export const EVENTS_NOTICE =
  "Check our latest events — upcoming workshops and webinars.";
